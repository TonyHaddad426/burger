// this is a functional component that allows us to output adjacent JSX elements 

// if there is no JSX in a file we don't need to import React from react



const aux = (props) => props.children;

export default aux;


